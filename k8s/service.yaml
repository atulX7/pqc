apiVersion: v1
kind: Service
metadata:
  name: pqc-readiness
  namespace: pqc
  labels:
    app: pqc-readiness
spec:
  type: LoadBalancer
  selector:
    app: pqc-readiness
  ports:
    - name: http
      port: 8080
      targetPort: 8080
